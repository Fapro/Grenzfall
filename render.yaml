services:
  - type: web
    name: wm2026-backend
    env: node
    rootDir: backend
    buildCommand: npm ci
    startCommand: npm start
    plan: free
    autoDeploy: true
    envVars:
      - key: NODE_VERSION
        value: 20
